#include <iostream>
#include <stdio.h>
#include<stdio.h>
#include <cstdlib>
#include <iostream>
#include<math.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	
	int N, X, X1, A;
	
	scanf("%d", &N);
	
	for(A = 1; A <= N; A+= 1)
	{
		
		if(A%2 == 0)
		{
		
		 X1 = A; X = A*A; printf("%d^2 = %d\n", X1, X);
		}
		else
		{
		}
				
	}
	
